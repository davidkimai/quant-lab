# Quant Lab Environment Configuration
# Copy this file to .env and update with your values

# =============================================================================
# APPLICATION
# =============================================================================
APP_NAME=Quant Lab API
APP_VERSION=0.1.0
DEBUG=true

# =============================================================================
# DATABASE
# =============================================================================
# Database type: sqlite (development) or postgresql (production)
DATABASE_TYPE=sqlite

# SQLite (default for local development)
DATABASE_URL=sqlite:///./quant_lab.db

# PostgreSQL (recommended for production)
# Uncomment and configure when using PostgreSQL
# DATABASE_TYPE=postgresql
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=quantlab
# POSTGRES_PASSWORD=quantlab
# POSTGRES_DB=quantlab

# =============================================================================
# API CONFIGURATION
# =============================================================================
# CORS origins (comma-separated list)
# For local development:
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# For production, set to your domain:
# CORS_ORIGINS=["https://yourdomain.com"]

# API prefix
API_PREFIX=/api

# =============================================================================
# WEB FRONTEND
# =============================================================================
# API base URL for frontend
VITE_API_URL=http://localhost:8000

# =============================================================================
# DOCKER COMPOSE (when using docker-compose)
# =============================================================================
# These are automatically set in docker-compose.yml
# Override here if needed for custom Docker setup

# POSTGRES_HOST=postgres
# POSTGRES_PORT=5432
# CORS_ORIGINS=["http://localhost:3000"]

# =============================================================================
# DATA PROVIDERS (optional)
# =============================================================================
# Add API keys for external data providers if needed
# ALPHA_VANTAGE_API_KEY=your_key_here
# POLYGON_API_KEY=your_key_here
# YAHOO_FINANCE_RATE_LIMIT=100

# =============================================================================
# LOGGING & MONITORING (optional)
# =============================================================================
# LOG_LEVEL=INFO
# SENTRY_DSN=your_sentry_dsn_here

# =============================================================================
# NOTES
# =============================================================================
# 1. Never commit the .env file to version control
# 2. For production, use strong passwords and secure database connections
# 3. Keep API keys secure and rotate regularly
# 4. SQLite is fine for development but use PostgreSQL for production
